export class categoryModel {
  // description: string;
  // createdDate: string;
  // completedDate: string;
  // active: string;
  // qa: string;;
  // idea: string;
  // name: string;
    /** không có trong request và response */
    createdDate: string;
    name: string;
    active: string;
    id: string;
    constructor(
     
    ) { }
  }
